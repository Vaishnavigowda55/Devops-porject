apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
  namespace: demo 
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1 Gi
